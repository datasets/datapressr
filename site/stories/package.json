{
  "name": "stories-charts-build",
  "private": true,
  "type": "module",
  "description": "Chart-build deps for the data stories (not published). Observable Plot rendered to static SVG at build time; jsdom supplies the DOM.",
  "dependencies": {
    "@observablehq/plot": "0.6.17",
    "jsdom": "30.0.1"
  }
}
