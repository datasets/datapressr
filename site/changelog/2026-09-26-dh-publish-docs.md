---
date: 2026-09-26
title: Publishing instructions updated for the current DataHub CLI
description: The push skill and docs now use dh publish, dh login and an explicit publication.
promote: false
---

The `push` skill and the publishing docs now use the DataHub CLI's `dh publish` command (the old `dh push` no longer works), with `dh login` for credentials, an explicit publication, and checks for the README and chart types DataHub needs to render a dataset page.
